#!/bin/sh
cd "${0%/*}" || exit 1

# Build in dependency order:
#   speciesTransport  (arrheniusProperty, trappingModel, speciesModel)
#   speciesCoupling   (BCs: sievertsCoupledMixed — needs speciesTransport)
#   speciesSolid      (solver module — needs both of the above)

wmake speciesTransport
wmake speciesCoupling
wmake speciesSolid
